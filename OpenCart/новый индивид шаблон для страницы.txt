1. Открываем страницу Тест (или создаём, если её нет).
2. Смотрим id ноды этой страницы в url. Например, https://ekb.czm.su/node/986/edit (смотрим не просто url страницы, а заходим в админку в редактирование этой страницы)
3. В /home/admeen/web/ekb.czm.su/public_html/sites/all/themes/flumb/templates создаём файл node-<id>.tpl.php В примере node-986.tpl.php.
4. В этот файл пишем вёрстку и css страницы по тз.

    4.1 Если нужно вставить блок, который есть в шорткоде (список шорткодов видно на любой странице редактирования):
    <?php
    // команда
    echo token_replace('[custom:reviews]', array('node' => $node));
    ?>

    4.2 Если нужно внести изменения в статический блок (то есть ниоткуда не тянутся фотки клиники/специалисты/отзывы и др сущности), просто копируем вёрстку и меняем в неё то, что нужно.

    4.3 Форма. На подобные страницы нужно найти форму с названием "кастомная форма" (или создать новую, если нет). 
        Кастомная форма ekb.czm.su
        4.3.1. Содержимое - Добавить содержимое - Webform
        4.3.2. Копируем из существующей формы поля, их настройки, почты и прочее. Запоминаем id формы (в url можно посмотреть)
        4.3.3. В структура - блоки автоматически создаётся блок опросник с называнием формы. Заголовок ставим <none>, чтобы он не выводился на страницу.
        4.3.4. На сервере sites/all/modules/amowebform/amowebform.module в массиве $form_data копируем структуру существующей формы, заменив необходимые данные на свои.
        4.3.5. Выводим блок через block_print('webform','client-block-<id формы>')
        Получаем форму без стилей на странице. В стилизации лучше не привязываться к классам с указанием id.

5. Потом меняем в node-<id>.tpl.php меняем id тестовой страницы на id нужной страницы